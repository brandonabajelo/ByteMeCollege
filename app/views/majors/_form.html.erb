<%= form_for(major) do |f| %>
  <% if major.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(major.errors.count, "error") %> prohibited this major from being saved:</h2>

      <ul>
      <% major.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :m_name %>
    <%= f.text_field :m_name %>
  </div>
  
  <%= fields_for(@departments) do |d| %>
  <div class="field">
    <%= collection_select(:department, :id, @departments, :id, :d_name, {}, {include_blank: true, :multiple => false, :prompt => 'Please select the author of this post'}) %>
  </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
